import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary
  static const Color primary = Color(0xFF001F5B);

  // Secondary
  static const Color secondary = Color(0xFF60A5FA);

  // Background
  static const Color background = Color(0xFFF5F7FD);

  // Card
  static const Color card = Colors.white;

  // Text
  static const Color textPrimary = Color(0xFF001F5B);
  static const Color textSecondary = Color(0xFF6B7280);

  // Border
  static const Color border = Color(0xFFE5E7EB);

  // Success
  static const Color success = Color(0xFF10B981);

  // Error
  static const Color error = Color(0xFFDC2626);

  // Chip
  static const Color chipBackground = Color(0xFFEAF2FF);

  // Navigation
  static const Color navSelected = Color(0xFF60A5FA);
  static const Color navUnselected = Color(0xFF6B7280);

  // Input
  static const Color inputBackground = Colors.white;

  // Status
  static const Color activeStudent = Color(0xFF60A5FA);
}